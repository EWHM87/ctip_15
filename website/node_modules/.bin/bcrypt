#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<< HEAD:mobile/node_modules/.bin/fingerprint
  exec "$basedir/node"  "$basedir/../@expo/fingerprint/bin/cli.js" "$@"
else 
  exec node  "$basedir/../@expo/fingerprint/bin/cli.js" "$@"
=======
  exec "$basedir/node"  "$basedir/../bcryptjs/bin/bcrypt" "$@"
else 
  exec node  "$basedir/../bcryptjs/bin/bcrypt" "$@"
>>>>>>> 6471a73e2b417d2fdb23f4a3769d6d75922eff66:website/node_modules/.bin/bcrypt
fi
